<script setup lang="ts">
useHead({ title: "FlutterFlowで爆速スマホアプリ — iOSとAndroidを同時に、素早く届ける" });
useSeoMeta({ description: "FlutterFlowを使って、iOSとAndroid両対応のスマホアプリを爆速で開発。従来の開発より短期間・低コストで、要件定義からリリースまで一貫して対応します。" });
</script>

<template>
  <div class="page">
    <header class="nav">
      <span class="nav-logo">FlutterFlow App</span>
      <a href="mailto:info@movee.jp" class="nav-cta">相談する →</a>
    </header>

    <!-- ヒーロー -->
    <section class="hero">
      <div class="hero-inner">
        <p class="hero-kicker">by movee · FlutterFlow × Flutter</p>
        <h1 class="hero-title">
          スマホアプリを、<br />爆速で届ける。
        </h1>
        <p class="hero-body">
          FlutterFlowを使って、iOS・Android両対応のアプリを短期間で開発します。
          アイデアをアプリにするまでの時間とコストを、大幅に圧縮します。
        </p>
        <a href="mailto:info@movee.jp" class="hero-btn">相談・お問い合わせ</a>
      </div>
    </section>

    <main>
      <!-- 課題 -->
      <section class="band band-white">
        <div class="inner">
          <p class="label">PROBLEM</p>
          <h2 class="heading">スマホアプリ開発で起きていること</h2>
          <div class="cards">
            <div class="card">
              <span class="card-num">01</span>
              <p class="card-title">開発コストが高く、予算が合わない</p>
              <p class="card-body">iOSとAndroidを別々に開発すると費用が2倍になります。中小企業や個人事業主にはハードルが高い状況です。</p>
            </div>
            <div class="card">
              <span class="card-num">02</span>
              <p class="card-title">リリースまで数ヶ月かかる</p>
              <p class="card-body">従来のネイティブ開発では要件定義から半年以上かかることも珍しくありません。市場投入のタイミングを逃します。</p>
            </div>
            <div class="card">
              <span class="card-num">03</span>
              <p class="card-title">仕様変更のたびに追加費用が発生する</p>
              <p class="card-body">リリース後の機能追加や修正が高コストになりがちです。事業の成長に合わせて柔軟に動けません。</p>
            </div>
          </div>
        </div>
      </section>

      <!-- FlutterFlowとは -->
      <section class="band band-gray">
        <div class="inner">
          <p class="label">SOLUTION</p>
          <h2 class="heading">FlutterFlowでできること</h2>
          <div class="feature-grid">
            <div class="feature">
              <p class="feature-title">iOS・Android同時対応</p>
              <p class="feature-body">1つのコードベースでiOSとAndroidの両方に対応したアプリを作れます。開発コストを大幅に削減できます。</p>
            </div>
            <div class="feature">
              <p class="feature-title">ビジュアル開発で高速化</p>
              <p class="feature-body">ドラッグ＆ドロップのビジュアル開発ツールを使い、UIの組み立てを効率化します。スクラッチ開発より大幅に短期間でリリースできます。</p>
            </div>
            <div class="feature">
              <p class="feature-title">Firebase連携でバックエンドも</p>
              <p class="feature-body">ユーザー認証・データベース・プッシュ通知までFirebaseと連携して構築します。バックエンドの開発期間も短縮できます。</p>
            </div>
            <div class="feature">
              <p class="feature-title">リリース後の改修も柔軟に</p>
              <p class="feature-body">機能追加や仕様変更がしやすい構成で作ります。事業の変化に合わせてアプリを育てていけます。</p>
            </div>
          </div>
        </div>
      </section>

      <!-- 前提条件 -->
      <section class="band band-white">
        <div class="inner">
          <p class="label">SCOPE</p>
          <h2 class="heading">対応範囲</h2>
          <div class="scope-grid">
            <div class="scope-col">
              <p class="scope-label">得意なアプリ</p>
              <ul class="scope-list">
                <li>予約・管理系アプリ</li>
                <li>社内業務ツール・勤怠管理</li>
                <li>会員サービス・ポイントアプリ</li>
                <li>マッチングアプリ（基本機能）</li>
                <li>ECアプリ（標準機能）</li>
              </ul>
            </div>
            <div class="scope-col">
              <p class="scope-label">対応内容</p>
              <ul class="scope-list">
                <li>要件定義・画面設計</li>
                <li>FlutterFlowによるUI開発</li>
                <li>Firebase連携・認証・DB設計</li>
                <li>App Store・Google Play申請サポート</li>
                <li>リリース後の運用・改修</li>
              </ul>
            </div>
          </div>
          <div class="scope-note">
            高度なカスタムアニメーション・ハードウェア連携・リアルタイム性が極めて高い処理はFlutterFlowの対応範囲外となる場合があります。ご相談ください。
          </div>
        </div>
      </section>

      <!-- 料金 -->
      <section class="band band-accent">
        <div class="inner">
          <p class="label label-light">PRICING</p>
          <h2 class="heading heading-light">料金</h2>
          <p class="price-note">要件・規模によって異なります。まずはご相談ください。</p>
          <div class="price-cards">
            <div class="price-card">
              <p class="price-name">スモールスタート</p>
              <p class="price-amount">¥300,000〜<span class="price-tax">税抜</span></p>
              <p class="price-desc">機能を絞ったMVP（最小限の製品）。まずリリースして反応を見たい場合に。</p>
            </div>
            <div class="price-card">
              <p class="price-name">スタンダード</p>
              <p class="price-amount">¥600,000〜<span class="price-tax">税抜</span></p>
              <p class="price-desc">認証・DB・プッシュ通知を含む一般的なアプリ構成。リリースまで対応します。</p>
            </div>
          </div>
        </div>
      </section>

      <!-- CTA -->
      <section class="band band-white cta-section">
        <div class="inner">
          <h2 class="cta-title">まずは相談だけでも、お気軽にどうぞ。</h2>
          <p class="cta-body">要件が固まっていなくても大丈夫です。アイデアの段階からお話しします。</p>
          <a href="mailto:info@movee.jp" class="cta-btn">info@movee.jp に相談する</a>
          <p class="cta-fine">返信は営業日2日以内を目安にしています</p>
        </div>
      </section>
    </main>

    <footer class="footer">
      <p>FlutterFlowで爆速スマホアプリ by movee</p>
    </footer>
  </div>
</template>

<style scoped>
.page {
  --accent:   #EA580C;
  --accent-h: #C2410C;
  --ink:      #0F172A;
  --ink-2:    #475569;
  --ink-3:    #94A3B8;
  --line:     #E2E8F0;
  --bg-gray:  #F8FAFC;

  background: #fff;
  color: var(--ink);
  font-family: -apple-system, BlinkMacSystemFont, "Hiragino Sans",
    "Hiragino Kaku Gothic ProN", "Noto Sans JP", sans-serif;
  line-height: 1.75;
  -webkit-font-smoothing: antialiased;
}

/* ── ナビ ─────────────────────────────────── */
.nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 18px 48px;
  border-bottom: 1px solid var(--line);
  background: #fff;
  position: sticky;
  top: 0;
  z-index: 10;
}
.nav-logo { font-size: 15px; font-weight: 800; letter-spacing: -0.03em; color: var(--ink); }
.nav-cta  { font-size: 13px; color: var(--accent); text-decoration: none; font-weight: 600; }
.nav-cta:hover { text-decoration: underline; }

/* ── ヒーロー ─────────────────────────────── */
.hero {
  background: var(--accent);
  padding: 88px 48px 96px;
  color: #fff;
}
.hero-inner { max-width: 680px; margin: 0 auto; }

.hero-kicker {
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: rgba(255,255,255,0.7);
  font-family: ui-monospace, monospace;
  margin: 0 0 24px;
}

.hero-title {
  font-size: clamp(36px, 5.5vw, 60px);
  font-weight: 900;
  letter-spacing: -0.05em;
  line-height: 1.1;
  margin: 0 0 28px;
  text-wrap: balance;
}

.hero-body {
  font-size: 16px;
  line-height: 1.85;
  color: rgba(255,255,255,0.85);
  margin: 0 0 40px;
  max-width: 540px;
}

.hero-btn {
  display: inline-block;
  background: #fff;
  color: var(--accent);
  text-decoration: none;
  font-size: 14px;
  font-weight: 700;
  padding: 14px 32px;
  border-radius: 4px;
  transition: opacity 0.15s;
}
.hero-btn:hover { opacity: 0.9; }

/* ── バンド共通 ───────────────────────────── */
.band       { padding: 72px 48px; }
.band-white { background: #fff; }
.band-gray  { background: var(--bg-gray); }
.band-accent { background: var(--accent); }

.inner { max-width: 800px; margin: 0 auto; }

.label {
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: var(--ink-3);
  font-family: ui-monospace, monospace;
  margin: 0 0 10px;
}
.label-light { color: rgba(255,255,255,0.65); }

.heading {
  font-size: 24px;
  font-weight: 800;
  letter-spacing: -0.03em;
  margin: 0 0 36px;
  color: var(--ink);
  text-wrap: balance;
}
.heading-light { color: #fff; }

/* ── 課題カード ───────────────────────────── */
.cards {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}
@media (max-width: 640px) { .cards { grid-template-columns: 1fr; } }

.card {
  padding: 24px 20px;
  border: 1px solid var(--line);
  border-radius: 8px;
}
.card-num {
  font-size: 11px;
  font-weight: 700;
  color: var(--accent);
  font-family: ui-monospace, monospace;
  display: block;
  margin-bottom: 10px;
}
.card-title { font-size: 15px; font-weight: 700; margin: 0 0 8px; color: var(--ink); }
.card-body  { font-size: 13px; color: var(--ink-2); margin: 0; line-height: 1.75; }

/* ── フィーチャーグリッド ──────────────────── */
.feature-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24px;
}
@media (max-width: 560px) { .feature-grid { grid-template-columns: 1fr; } }

.feature {
  padding: 24px;
  background: #fff;
  border-radius: 8px;
  border: 1px solid var(--line);
}
.feature-title { font-size: 15px; font-weight: 700; margin: 0 0 8px; color: var(--ink); }
.feature-body  { font-size: 13px; color: var(--ink-2); margin: 0; line-height: 1.75; }

/* ── スコープ ─────────────────────────────── */
.scope-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 32px;
  margin-bottom: 20px;
}
@media (max-width: 560px) { .scope-grid { grid-template-columns: 1fr; } }

.scope-label {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.08em;
  color: var(--accent);
  font-family: ui-monospace, monospace;
  margin: 0 0 12px;
  text-transform: uppercase;
}
.scope-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.scope-list li {
  font-size: 14px;
  color: var(--ink-2);
  padding-left: 14px;
  position: relative;
}
.scope-list li::before {
  content: "—";
  position: absolute;
  left: 0;
  color: var(--ink-3);
  font-size: 11px;
}
.scope-note {
  font-size: 12px;
  color: var(--ink-3);
  line-height: 1.7;
  padding: 14px 16px;
  background: var(--bg-gray);
  border-left: 3px solid var(--line);
  border-radius: 0 4px 4px 0;
}

/* ── 料金 ─────────────────────────────────── */
.price-note {
  font-size: 14px;
  color: rgba(255,255,255,0.8);
  margin: -20px 0 28px;
}
.price-cards {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}
@media (max-width: 520px) { .price-cards { grid-template-columns: 1fr; } }

.price-card {
  background: rgba(255,255,255,0.12);
  border: 1px solid rgba(255,255,255,0.2);
  border-radius: 10px;
  padding: 24px;
}
.price-name   { font-size: 11px; font-weight: 700; color: rgba(255,255,255,0.7); margin: 0 0 8px; letter-spacing: 0.08em; text-transform: uppercase; font-family: ui-monospace, monospace; }
.price-amount { font-size: 32px; font-weight: 900; letter-spacing: -0.04em; color: #fff; margin: 0 0 10px; line-height: 1; font-variant-numeric: tabular-nums; }
.price-tax    { font-size: 12px; font-weight: 400; opacity: 0.7; margin-left: 4px; vertical-align: middle; }
.price-desc   { font-size: 13px; color: rgba(255,255,255,0.75); margin: 0; line-height: 1.7; }

/* ── CTA ──────────────────────────────────── */
.cta-section .inner { text-align: center; }
.cta-title { font-size: 24px; font-weight: 800; letter-spacing: -0.03em; margin: 0 0 14px; text-wrap: balance; }
.cta-body  { font-size: 15px; color: var(--ink-2); margin: 0 0 32px; }
.cta-btn {
  display: inline-block;
  background: var(--accent);
  color: #fff;
  text-decoration: none;
  font-size: 15px;
  font-weight: 700;
  padding: 15px 36px;
  border-radius: 4px;
  transition: background 0.15s;
}
.cta-btn:hover { background: var(--accent-h); }
.cta-fine { font-size: 12px; color: var(--ink-3); margin: 14px 0 0; }

/* ── フッター ─────────────────────────────── */
.footer {
  border-top: 1px solid var(--line);
  padding: 24px 48px;
  font-size: 12px;
  color: var(--ink-3);
  background: #fff;
  font-family: ui-monospace, monospace;
}

@media (max-width: 600px) {
  .nav    { padding: 14px 20px; }
  .hero   { padding: 60px 20px 72px; }
  .band   { padding: 56px 20px; }
  .footer { padding: 20px; }
}
</style>
